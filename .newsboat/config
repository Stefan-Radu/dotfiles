# Bindings
bind-key j down
bind-key k up
bind-key J next-feed articlelist
bind-key K prev-feed articlelist
bind-key G end
bind-key g home
bind-key d pagedown
bind-key u pageup
bind-key l open
bind-key h quit
bind-key a toggle-article-read
bind-key n next-unread
bind-key N prev-unread
bind-key U show-urls
bind-key ; macro-prefix

# Other stuff
browser librewolf
macro y set browser "mpv %u" ; open-in-browser ; set browser librewolf
macro w set browser "w3m" ; open-in-browser ; set browser librewolf
macro f set browser "feh" ; open-in-browser ; set browser librewolf
